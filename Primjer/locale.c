#include <stdio.h>
char t[] = {0,0,0,0};
int isalnum(int c) {
    //return c >= '0' && c <= '9' || c >= 'A' && c <= 'Z' || c >= 'a' && c <= 'z';
    return t[c];
}
int a;
int f(int a, int b) {
    
}
int main(void) {
    int p[10];
    printf ("%u %u %u", p, p+1, p+2);


    //char a = '�';
    //printf("%s ", _get_current_locale());
    // setlocale (LC_ALL, "hr-HR.iso88592");
    // printf ("%c %c\n", a, toupper(a));
    printf ("%d\n", 0b1000000);
}
